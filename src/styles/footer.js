const colorSpan = '#bdbdbd'

const footer = {
    container: {
        marginTop: '10px',
        height: '100px',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        width: '100%'
    },
    parrafo: {
        color: colorSpan,
        fontWeight: 500
    },
    enlace: {
        textDecoration: 'none',
        fontWeight: 700,
        color: colorSpan
    }
}
export default footer